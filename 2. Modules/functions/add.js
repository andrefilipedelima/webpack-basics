/* function add(a, b) {
    return a + b; 
}

function addAndMultiply(a,b,c) {
    return (a + b) * c;
}

module.exports = {
    add,
    addnMultiply: addAndMultiply,
}; */

function add(a, b) {
    return a + b; 
}

module.exports = add;